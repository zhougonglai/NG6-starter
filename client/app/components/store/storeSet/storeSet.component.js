import template from './storeSet.html';
import controller from './storeSet.controller';
import './storeSet.scss';

let storeSetComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default storeSetComponent;
