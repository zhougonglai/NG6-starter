import template from './storeInfo.html';
import controller from './storeInfo.controller';
import './storeInfo.scss';

let storeInfoComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default storeInfoComponent;
